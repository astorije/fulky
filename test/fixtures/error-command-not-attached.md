# Error - Inline commands not directly after its code block

<!-- fulky:skip-test -->

Inline commands such as `skip-test` should always be followed by a code block.

```js
require('assert').ok(true);
```
